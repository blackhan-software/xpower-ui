header
    nav#menu.nav.nav-pills.nav-justified.flex-column.flex-sm-row.mb-3
        a.flex-sm-fill.text-sm-center.nav-link.home(
            href="/home?token={{TOKEN}}" class=`${PAGE === 'home' ? 'active' : ''}`
        )
            i.bi-lightning-charge-fill.float-start
            span: | XPower
        a.flex-sm-fill.text-sm-center.nav-link.migrate(
            href="/migrate?token={{TOKEN}}" class=`${PAGE === 'migrate' ? 'active' : ''}`
        )
            i.bi-arrow-left-right.float-start
            span: | Migrate
        a.flex-sm-fill.text-sm-center.nav-link.about(
            href="/about?token={{TOKEN}}"  class=`${PAGE === 'about' ? 'active' : ''}`
        )
            i.bi-lightbulb-fill.float-start
            span: | About
