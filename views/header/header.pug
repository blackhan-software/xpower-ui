header
    nav#menu.nav.nav-pills.nav-justified.mb-3
        a.btn.btn-outline-warning.flex-sm-fill.text-sm-center.nav-link.home(
            class=`${PAGE === 'home' ? 'active' : ''}`
            href="/home?token={{TOKEN}}"
        )
            i.bi-lightning-charge-fill.float-sm-start
            span.d-none.d-sm-inline: | XPower
        a.btn.btn-outline-warning.flex-sm-fill.text-sm-center.nav-link.nfts(
            class=`${PAGE === 'nfts' ? 'active' : ''}`
            href="/nfts?token={{TOKEN}}"
        )
            i.bi-image-fill.float-sm-start
            span.d-none.d-sm-inline: | NFTs
        a.btn.btn-outline-warning.flex-sm-fill.text-sm-center.nav-link.staking(
            class=`${PAGE === 'staking' ? 'active' : ''}`
            href="/staking?token={{TOKEN}}"
        )
            i.bi-cash-stack.float-sm-start
            span.d-none.d-sm-inline: | Staking
        a.btn.btn-outline-warning.flex-sm-fill.text-sm-center.nav-link.about(
            class=`${PAGE === 'about' ? 'active' : ''}`
            href="/about?token={{TOKEN}}"
        )
            i.bi-lightbulb-fill.float-sm-start
            span.d-none.d-sm-inline: | About
