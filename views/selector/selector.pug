form#selector
  div.btn-group.selectors(
    role="group"
  )
    a.btn.btn-outline-warning.selector-thor(
      type="button" data-bs-toggle="tooltip" data-bs-placement="top"
      href="?token=THOR" title="XPower THOR" data-bs-fixed="true"
      class="{{SELECT0R_THOR}}"
    )
      span.spinner.spinner-border.spinner-border-sm.float-sm-start(
        role="status" style="display:none"
      )
      img.float-sm-start(
          alt="THOR"
          class="{{xtoken}}"
          height="24" width="24"
          src=`/images/svg/thor-black.svg`
      )
      span.d-none.d-sm-inline: | THOR
    a.btn.btn-outline-warning.selector-loki(
      type="button" data-bs-toggle="tooltip" data-bs-placement="top"
      href="?token=LOKI" title="XPower LOKI" data-bs-fixed="true"
      class="{{SELECT0R_LOKI}}"
    )
      span.spinner.spinner-border.spinner-border-sm.float-sm-start(
        role="status"  style="display:none"
      )
      img.float-sm-start(
          alt="LOKI"
          class="{{xtoken}}"
          height="24" width="24"
          src=`/images/svg/loki-black.svg`
      )
      span.d-none.d-sm-inline: | LOKI
    a.btn.btn-outline-warning.selector-odin(
      type="button" data-bs-toggle="tooltip" data-bs-placement="top"
      href="?token=ODIN" title="XPower ODIN" data-bs-fixed="true"
      class="{{SELECT0R_ODIN}}"
    )
      span.spinner.spinner-border.spinner-border-sm.float-sm-start(
        role="status" style="display:none"
      )
      img.float-sm-start(
          alt="ODIN"
          class="{{xtoken}}"
          height="24" width="24"
          src=`/images/svg/odin-black.svg`
      )
      span.d-none.d-sm-inline: | ODIN
