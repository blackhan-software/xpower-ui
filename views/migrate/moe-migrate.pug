form.moe-migrate
  div.btn-group.moe-migrate(role="group" class="{{xtoken}}")
    button.approve-moe-allowance.btn.btn-outline-warning(
      data-source="{{VERSION_SRC}}" data-target="{{VERSION_TGT}}"
      type="button" class="{{xtoken}}" disabled
    )
      | Approve <span class="d-none d-sm-inline">Allowance</span>
    button.moe-migrate.btn.btn-outline-warning(
      data-source="{{VERSION_SRC}}" data-target="{{VERSION_TGT}}"
      type="button" class="{{xtoken}}" disabled
    )
      | Migrate
    button.btn.btn-outline-warning.info(
      type="button" data-bs-toggle="tooltip" data-bs-placement="top"
      title="Approve & migrate the old {{xTOKEN}} tokens"
    )
      include ../../public/images/pug/info-circle-fill.svg.pug
