import { nice, nice_si, trim } from '../source/functions';
export { nice, nice_si, trim };
import { md_render } from './md-render';
export { md_render };

export default {
    md_render, nice, nice_si, trim
};
